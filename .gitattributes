# SPDX-License-Identifier: Apache-2.0
#
# Forces LF on every text file: *.sh scripts run inside a Linux
# devcontainer/target host, and a CRLF shebang fails to exec.
* text=auto eol=lf

# Leave certs byte-exact rather than trusting text auto-detection.
*.pem -text
