# proxmox_url/proxmox_api_token_id/proxmox_api_token_secret are NOT listed
# here — inside the devcontainer they're auto-populated from
# PROXMOX_VE_ENDPOINT/PROXMOX_VE_API_TOKEN. Set them here only if building
# outside that devcontainer:
# proxmox_url               = "https://pve.example.com:8006/api2/json"
# proxmox_api_token_id      = "root@pam!packer"
# proxmox_api_token_secret  = "REPLACE_ME"

# k8s_version/cilium_version/argocd_version are deliberately NOT set here —
# build.sh resolves them automatically before every build. A var-file entry
# of the same name would silently outrank that resolution; see README.md.

proxmox_node                     = "pve"
seed_template_vm_id              = 9000
disk_datastore_id                = "local-zfs"
proxmox_insecure_skip_tls_verify = true
